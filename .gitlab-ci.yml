include:
  - project: 'project-templates/ci-configurations'
    ref: main
    file: '/serverless.golang.yaml'

deploy:prod:
  variables:
    ENVIRONMENT: prod
    AWS_PROFILE: onz
  environment:
    name: production
  only:
    - main

list prod deploys:
  variables:
    AWS_PROFILE: onz
